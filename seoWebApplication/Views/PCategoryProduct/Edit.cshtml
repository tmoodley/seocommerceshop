@model seoWebApplication.Data.pcategoryproduct

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_BaseAdminSingle.cshtml";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>pcategoryproduct</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.pcategoryFK, "pcategoryFK", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("pcategoryFK", String.Empty)
                @Html.ValidationMessageFor(model => model.pcategoryFK)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.productFK, "productFK", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("productFK", String.Empty)
                @Html.ValidationMessageFor(model => model.productFK)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
